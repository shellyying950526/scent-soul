
{
  "name": "scent-soul",
  "version": "1.0.0",
  "private": true,
  "description": "AI 面相香水推荐师",
  "scripts": {
    "start": "npx serve .",
    "build": "mkdir -p .vercel/output/static && cp -r * .vercel/output/static || true"
  }
}
