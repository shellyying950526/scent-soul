
{
  "name": "scent-soul",
  "version": "1.0.0",
  "private": true,
  "description": "AI 面相香水推荐师",
  "scripts": {
    "start": "npx serve .",
    "build": "mkdir -p public && cp index.html index.tsx types.ts metadata.json public/ && cp -rf components services public/ 2>/dev/null || true"
  }
}
